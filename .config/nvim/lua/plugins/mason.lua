return {
    url = "https://github.com/mason-org/mason.nvim",
    cond = true,

    opts = {
        ui = {
            width = 0.75,
            height = 0.75,
        }
    },

    lazy = false, -- Lazy-load plugin
}
